function cal(a,b,res)
{
    if(res=='sum'){
    return a+b;}
   else if(res=='sub'){
    return a-b;}
  else  if(res=='mult'){
    return a*b;}
  else  if(res=='div'){
    return a/b;}
else{return 'Not find'}
}